cmake_minimum_required(VERSION 3.17)
project(nbtp)
include_directories(lodepng)
include_directories(curl)
find_library(curl curl)
find_library(cjson cjson)
link_libraries(curl)
link_libraries(cjson)
link_libraries(m)
add_executable(nbtp main.c upload.c lodepng.c process.c codec.c)